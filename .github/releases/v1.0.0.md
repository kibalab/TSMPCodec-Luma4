# TSMP Codec Luma4 1.0.0

Stable release consolidating **all 0.0.4-beta.1 through 0.0.4-beta.2 changes since stable 0.0.3**. Runtime, shaders and asset GUIDs are unchanged from the latest beta (0.0.4-beta.2); this promotion updates metadata, dependencies and documentation.

## Cumulative Changes

- Use a 16-entry Float32 calibration LUT for effective sample sizes greater than one; retain the original single-sample path.
- Bundle the preparation shader/material on the codec prefab and retain ordinary decoding when preparation resources are unavailable with a compatible Core.
- Reuse native encoder raster arrays and explicitly opt in to Core's GPU Luma4 writer. Retain CPU fallback and preserve the existing palette.
- Write decoded header and payload bytes directly to the combined readback target, retaining the legacy shader path.
- Target stable Core 1.0.0 through UPM and >=1.0.0 through VPM. Earlier beta dependencies moved from Core 0.2.0 to the preparation API in 0.3.0-beta.2 and then the optimized pipeline in beta.3; the stable baseline now supersedes those beta requirements.
- Preserve codec IDs, packet layouts, existing script/material/prefab GUIDs and quantization behavior.
- Update Japanese, English and Korean installation/compatibility documentation. Add English issue templates and contribution guidance.

## Installation and Compatibility

- Install **Core 1.0.0** first. Use **Luma4 1.0.0** as the default/header codec; optional RGB16, RGB20 and Color256 stable releases are **2.0.0**.
- VCC: install the stable packages from https://vpm.kiba.red/. Prerelease visibility is not required.
- Ordinary Unity: install Core and the codec packages using UPM Add package from disk or explicit package sources. VRCSDK is not required. Package metadata alone does not tell UPM to fetch Core from GitHub.
- VRChat uses VPM's Worlds SDK >=3.9.0 dependency. Allow Unity to refresh assets and recompile UdonSharp after updating; the shared Controller workflow remains automatic.
- Missing preparation materials can select the legacy shader path, but cannot compensate for an older Core API. These package sources require Core 1.0.0.
- The authoritative GPU-encoded image is the Encoder output RenderTexture; hidden CPU staging outputTexture is not refreshed by the GPU path.

## Validation and Performance

- Included paired Core/Luma4 measurements: Udon encoder mean 11.27 -> 0.457 ms (24.7x), receiver submission/callback means 3.904 -> 0.791 ms (4.9x), at 720p / 4 KiB / 60 Hz. These are pipeline measurements against the pre-resource-optimization baseline, **not an isolated Luma4 0.0.3-versus-1.0.0 comparison**.
- The Udon 360p / 32 B raster helper improved from 0.158 to 0.066 ms. GPU encoding adds a conversion draw: 720p Gamma conversion/expansion median 16.5 us versus 8.3 us for expansion alone, excluding transfers/readback.
- Native Gamma/Linear and compiled Udon VM GPU/CPU pixel comparisons passed. CPU fallback remains available.
- Public beta archives were checked against their release tags and VPM hashes, then tested together in isolated SDK-free and SDK-present projects.
- Unity 2022.3.22f1, Windows/D3D11, RTX 4090; native Windows x64 Development Mono Player; Worlds 3.10.4-beta.2 with bundled UdonSharp and the SDK Editor VM. Compiling UdonSharp and running the SDK VM do not prove live VRChat client behavior.
- Live VRChat, Quest/mobile, other GPUs/APIs, OBS/MediaMTX transport and IL2CPP/stripping remain unverified. The documented legacy Linear loopback harness limitation is not certified by separate passing pixel tests.

## History and Evidence

- [All changes since the previous stable release](https://github.com/kibalab/TSMPCodec-Luma4/compare/v0.0.3...v1.0.0)
- [Package changelog with every beta entry](https://github.com/kibalab/TSMPCodec-Luma4/blob/v1.0.0/Packages/com.kibalab.tsmp.codec.luma4/CHANGELOG.md)
- [Core 1.0.0 cumulative release, measurements and limitations](https://github.com/kibalab/TSMP-Core/releases/tag/v1.0.0)
- [Public beta artifact and runtime verification](https://github.com/kibalab/TSMP-Core/blob/v1.0.0/Validation~/PackageCompatibility/PUBLISHED-20260919.md)
